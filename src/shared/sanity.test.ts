import { describe, expect, it } from 'vitest';

// Placeholder — Vitest wiring is exercised here until modules/billing,
// modules/identity and modules/licensing have real logic to cover
// (PLANNING.md §5).
describe('scaffold sanity', () => {
	it('runs', () => {
		expect(1 + 1).toBe(2);
	});
});
